name: Build and release CaspaHub app

# This repo is public specifically so these jobs get free/unlimited GitHub Actions
# minutes even while the main account's billing is blocked (see README) — the actual
# source lives in the private ken-muritu/caspahub-flutter repo and is pulled in below via
# a read-only deploy token, never committed here.
on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Branch/tag/sha in caspahub-flutter to build"
        required: false
        default: "main"
      release_tag:
        description: "Tag to publish the build under in THIS repo's Releases"
        required: true

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout private source
        uses: actions/checkout@v4
        with:
          repository: ken-muritu/caspahub-flutter
          ref: ${{ inputs.ref }}
          token: ${{ secrets.SOURCE_REPO_PAT }}

      - uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Scaffold platform folders if missing
        run: |
          if [ ! -d android ]; then
            flutter create --platforms=android --org com.caspahub --project-name caspahub_app .
          fi

      - name: Install dependencies
        run: flutter pub get

      # Writes the real CaspaHub mark into android's mipmap-*/, overwriting the default
      # Flutter icon `flutter create` just generated above. Config in pubspec.yaml under
      # `flutter_launcher_icons:`.
      - name: Generate launcher icon
        run: dart run flutter_launcher_icons

      - name: Enable R8 shrinking
        run: python3 tool/patch_android_release.py

      - name: Patch manifest for in-app updates
        run: python3 tool/patch_android_manifest.py

      # See tool/patch_android_signing.py — without this, release builds sign with a
      # different random debug keystore on every CI run (ephemeral runners, nothing under
      # ~/.android persists), and Android refuses to install an update signed with a
      # different key than what's already on the device ("App not installed").
      - name: Decode release keystore
        run: echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > android/app/release.keystore.p12

      - name: Wire persistent release signing
        run: python3 tool/patch_android_signing.py

      - name: Analyze
        run: flutter analyze

      - name: Build APK (release, split per ABI)
        env:
          ANDROID_KEYSTORE_PATH: release.keystore.p12
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: flutter build apk --release --split-per-abi

      - uses: actions/upload-artifact@v4
        with:
          name: apk-arm64
          path: build/app/outputs/flutter-apk/app-arm64-v8a-release.apk

      - uses: actions/upload-artifact@v4
        with:
          name: apk-armv7
          path: build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk

  publish:
    needs: build-android
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Publish release in this (public) repo
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # auto-<sha> tags (the hourly poller) are marked --prerelease so GitHub's
          # releases/latest (and therefore the website's direct-download link, which
          # points at releases/latest/download/<filename> — see caspahub-booking's
          # install-prompt.tsx) always resolves to the most recent deliberately-cut
          # vX.Y.Z release, never an unvetted auto-build.
          PRERELEASE_FLAG=""
          if [[ "${{ inputs.release_tag }}" == auto-* ]]; then
            PRERELEASE_FLAG="--prerelease"
          fi
          gh release create "${{ inputs.release_tag }}" \
            dist/apk-arm64/app-arm64-v8a-release.apk \
            dist/apk-armv7/app-armeabi-v7a-release.apk \
            --repo "${{ github.repository }}" \
            --title "${{ inputs.release_tag }}" \
            $PRERELEASE_FLAG \
            --notes "Built from ken-muritu/caspahub-flutter@${{ inputs.ref }}. Source is private; this repo only hosts the build+release while GitHub Actions billing is blocked on the main account."
